import { StyleSheet } from "react-native";

export const tabStyles = StyleSheet.create({
	tabBar: {
		height: 68,
		paddingBottom: 10,
		paddingTop: 8,
	},
	tabLabel: {
		fontSize: 12,
		fontWeight: "600",
	},
});
